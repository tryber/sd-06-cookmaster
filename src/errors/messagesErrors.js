const INVALID_ENTRIES = 'Invalid entries. Try again.';
const USER_UNAUTHORIZED = 'All fields must be filled';
const INCORRECT_USERNAME = 'Incorrect username or password';
const EMAIL_CONFLICT = 'Email already registered';

module.exports = {
  INVALID_ENTRIES,
  USER_UNAUTHORIZED,
  INCORRECT_USERNAME,
  EMAIL_CONFLICT,
};
