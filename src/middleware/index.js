const validingNewUser = require('./validingNewUser');
const checkEmail = require('./checkEmail');
const validingEmail = require('./validingEmail');
const validingLogin = require('./validingLogin');
const validingRecipesField = require('./validingRecipesField');
const validingRecipes = require('./validingRecipes');
const validingPermissions = require('./validingPermissions');
const validingAdm = require('./validingAdm');

module.exports = {
  validingNewUser,
  checkEmail,
  validingEmail,
  validingLogin,
  validingRecipesField,
  validingRecipes,
  validingPermissions,
  validingAdm,
};
