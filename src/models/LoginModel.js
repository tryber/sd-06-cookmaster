// const connection = require('./connection');

// const userLogin = async (email) => connection()
//   .then((db) => db.collection('users').findOne({ email }));

// module.exports = {
//   userLogin,
// };
